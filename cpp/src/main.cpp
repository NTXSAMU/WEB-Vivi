// src/main.cpp — placeholder del módulo nativo.
// De momento solo confirma que el toolchain de CMake está bien configurado.
// Sustituye este contenido por la lógica real (física, DSP de audio, etc.)
// cuando la necesites.
#include <iostream>

#include "module.hpp"

int main() {
    std::cout << "mi-landing :: módulo C++ listo (" << mi_landing::version() << ")\n";
    return 0;
}
