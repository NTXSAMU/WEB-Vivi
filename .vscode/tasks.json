{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Flask backend",
      "type": "shell",
      "command": "python app.py",
      "options": { "cwd": "${workspaceFolder}/backend" },
      "problemMatcher": [],
      "group": { "kind": "build", "isDefault": true }
    },
    {
      "label": "Run backend tests",
      "type": "shell",
      "command": "pytest tests/backend -v",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    }
  ]
}
