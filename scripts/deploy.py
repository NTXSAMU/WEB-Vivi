#!/usr/bin/env python3
"""
deploy.py — placeholder de despliegue.

Aún no hay un destino de despliegue definido (Render, Fly.io, VPS propio...).
Cuando lo decidas, este script debería:
  1. Ejecutar build.py
  2. Correr los tests (tests/backend)
  3. Subir el resultado al servicio elegido
"""


def main() -> None:
    print("deploy.py: todavía no hay un target de despliegue configurado.")
    print("Edita este script cuando decidas dónde vas a alojar el proyecto.")


if __name__ == "__main__":
    main()
